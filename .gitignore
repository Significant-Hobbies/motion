# Node
node_modules/
.wrangler/
dist/
coverage/
.parcel-cache/
*.tsbuildinfo

# PartyKit
.partykit/

# Vite
web/dist/

# iOS / Xcode
ios/Motion.xcodeproj/
ios/*.xcworkspace/
ios/build/
ios/DerivedData/
xcuserdata/
*.xcuserstate
.DS_Store

# CocoaPods — vendored MediaPipe deps. Podfile + Podfile.lock ARE committed (reproducible
# installs); the fetched Pods/ tree is regenerated by `pod install` and is not.
ios/Pods/

# Env / local
.env
.env.local
*.log

# Foundry evidence — generated by ./scripts/foundry-evidence.sh and CI; not source.
foundry-evidence.json

site/node_modules/
site/dist/
site/.astro/
